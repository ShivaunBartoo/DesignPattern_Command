# DesignPattern_Command

A simple Java implementation of the Command design pattern for BCIT CST COMP 2522. 

## Overview

This project showcases the Command pattern by allowing users to append text to a document, undo their last action, and redo undone actions. The core logic is managed by the `Command`, `CommandStack`, and `Main` classes.
